<% @profiles.each do |profile| %>

    <div class="container">
        <div class="card">

            <div class="image">
                <%= image_tag(profile.image_data, alt: "photo") %>
            </div>

            <div class="text">
                <div class="fab">&#43;</div>
                <h3><%= "#{profile.first_name} #{profile.last_name}" %></h3>
                </br>
                <p><%= "Lives in: #{profile.city}, #{profile.state}." %></p>
                </br>
                <p><%= "Services offered by #{profile.first_name}:" %></p>
                </br>
                <p><% Service.where(user_id: profile.user_id).each do |service| %></p>
                    <strong><%= "#{service.title}" %></strong>
                    </br>
                    <p> <%= link_to 'See service', service_path(:id => service.id), :class => "link_to" %></p>
                    </br>
                <% end %>
                <p>

            </div>

        </div>
    </div>

<% end %>
